//code調べて参考にする



/*
import React, {Component} from 'react';
import Snackbar from '@mui/material/Snackbar';
import MuiAlert from '@mui/material/Alert';


// ToastMessages.js
import React from 'react';
import Snackbar from '@mui/material/Snackbar';
import MuiAlert from '@mui/material/Alert';

class ToastMsg {
    const success = (message) => {

    }
}


const Alert = React.forwardRef(function Alert(props, ref) {
  return <MuiAlert elevation={6} ref={ref} variant="filled" {...props} />;
});

export class ToastMessages {
  static context = null;

  static setSnackbarContext(context) {
    ToastMessages.context = context;
  }

  static success(message) {
    if (ToastMessages.context) {
      ToastMessages.context.showMessage(message, 'success');
    }
  }

  static error(message) {
    if (ToastMessages.context) {
      ToastMessages.context.showMessage(message, 'error');
    }
  }

  static warning(message) {
    if (ToastMessages.context) {
      ToastMessages.context.showMessage(message, 'warning');
    }
  }
}

*/